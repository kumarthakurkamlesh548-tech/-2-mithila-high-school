rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Require user to be logged in to read documents/photos
      allow read: if request.auth != null;
      // Allow authenticated staff/users to upload assets
      allow write: if request.auth != null;
    }
  }
}
